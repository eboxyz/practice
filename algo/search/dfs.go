package search

// (TODO): come back to this
// func DFS(node int, nodes map[int][]int, fn func(int)) {
// 	dfsRecur(node, map[int]bool{}, fn)
// }

// func dfsRecur(node int, v map[int]bool, fn func(int)) {
// 	//for the initial node, we want the search to be true
// 	v[node] = true
// 	//not sure what this callback is doing
// 	fn(node)
// 	for _, n := range nodes[node] {
// 		if _, ok := v[n]; !ok {
// 			dfsRecur(n, v, fn)
// 		}
// 	}
// }
